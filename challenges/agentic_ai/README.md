This challenge requires you to indirectly prompt-inject an LLM-based coding agent via a private developer forum.

This challenge is a single Docker image (not a `compose`), but only if you can point the Docker image at a running Ollama image. (Hint: `docker run -it --rm ollama/ollama` and then point `OLLAMA_HOST` to that).
