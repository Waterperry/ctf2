This challenge involves exploiting a chosen plaintext-style attack on a known embedding model, in order to extract
the target's "memorable date" from an embedding stolen from an embedding database. The only data required for the
challenge are `model.zip`, `tokenizer.zip`, and `target.pt` which is the embedding model, tokenizer and target
embedding respectively.
